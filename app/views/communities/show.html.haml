- content_for :title do
  = "#{t('communities.show.overview')} '#{@community.name}'"
- content_for :menu  do
  = render partial: 'menu.html.haml'

= render partial: 'invitations' unless @invitations.empty?