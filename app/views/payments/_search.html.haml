%ul.nav.pull-right
  %li
    = form_tag '', :method => :get, :class => 'navbar-search pull-right' do
      = text_field_tag :q, (params.has_key?(:q) ? params[:q] : t('.search-payments')), class: 'search-query'
  %li.divider-vertical