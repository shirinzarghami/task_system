%tr
  %td= community.name
  %td= community.subdomain
  %td= community.members.count
  %td
    =link_to 'Edit', edit_admin_community_path(community)
    =link_to 'Delete', admin_community_path(community), method: :delete, confirm: "Are you sure?"