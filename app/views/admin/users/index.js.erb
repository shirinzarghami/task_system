$(".filter").each(function(){
    $(this).removeClass('active');
});
$('#<%= @filter %>').addClass('active');
$('#<%= @filter %>').animate({'background-color':'red'}, 2000);
  $('#users').fadeOut('slow', function() {
    $('#users').html("<%= j render partial: 'users' %>");
    $('#users').fadeIn('slow');
  });